minikube start --nodes 3 -p fleet-cluster
kubectl get nodes
kubectl apply -f api.yaml
kubectl apply -f producer.yaml
kubectl apply -f consumer.yaml
kubectl get pods -o wide
kubectl delete pod <NOM_D_UN_POD_API>
kubectl get pods # Le nouveau pod est déjà en cours de création !
# Trouve le nom du pod
kubectl get pods

# Connecte-toi (remplace par ton nom de pod)
kubectl exec -it kafka-5c9b844fd8-fhcvg  -- sh
/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list
/opt/kafka/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic telemetry --from-beginning

kubectl apply -f kafka-deployment.yaml
kubectl apply -f kafka-init-job.yaml
kubectl apply -f producer.yaml
kubectl apply -f consumer.yaml
kubectl apply -f api.yaml
kubectl get pods
kubectl get services
kubectl get jobs

# Voir les 3 replicas API
kubectl get pods -l app=api

# Round-robin sur /whoami
for i in $(seq 1 9); do
  curl -s http://<IP_NODE>:30500/whoami
  echo
done

# Scaler
kubectl scale deployment api --replicas=5
kubectl get pods -l app=api